defaults:
  - _self_
  - clip: default
  - data: default
  - training: default
  - hydra: default

checkpoint_path: lightning_logs/version_2/checkpoints/best-val-model-epoch=039-val_loss=0.511.ckpt
output_dir: inference_results

sample_id: null # If null, process all samples
text_prompt: null # If null and sample has caption, will use sample caption

device: cuda
