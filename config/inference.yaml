defaults:
  - _self_
  - clip: default
  - data: default
  - training: default
  - hydra: default

checkpoint_path: lightning_logs/version_8/checkpoints/best-val-model-epoch=049-val_loss=0.022.ckpt
output_dir: inference_results

sample_id: null # If null, process all samples
text_prompt: null # If null and sample has caption, will use sample caption

device: cuda
